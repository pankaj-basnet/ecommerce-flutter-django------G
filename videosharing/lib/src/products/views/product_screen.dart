import 'package:flutter/material.dart';

class ProductPage extends StatelessWidget {
  const ProductPage({super.key, required this.productId});

  final String productId; // not used ----- isn=

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
